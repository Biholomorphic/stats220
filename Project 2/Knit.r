# Literally created this file because I hate Rstudio
# and wanted to configure R to work in VS code but
# can't be bothered remembering the commands to knit
# the rmd file from terminal, so I just run this instead
# lmao.
library(rmarkdown)

rmarkdown::render("dynamic_report.Rmd")